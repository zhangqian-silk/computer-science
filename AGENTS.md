# 仓库编辑规范

本文件统一规定文档的写作、展示与编辑方式。目标是核心内容成体系、表达清楚、长期维护复杂度最低。子目录规范只补充领域要求，不重复维护通用规则。

---

## 写作风格

- 开头直接定义对象并交代核心问题，不写宣传性评价、写作意图或泛泛的背景铺垫。
- 正文优先采用「定义 — 分类 — 机制 — 边界 — 取舍」的顺序。先说明对象是什么，再说明分为哪几类、各类如何工作、适用条件和代价是什么。
- 说明机制时写清因果链：问题因何产生、机制依赖什么条件、输入如何变化、后续流程受到什么影响。不要只罗列术语或特性。
- 比较多种方案时，先给出共同问题，再沿同一组维度展开，例如作用范围、生命周期、维护方式、一致性要求、性能开销、故障模式和适用条件。优缺点必须能从机制推出。
- 语言专业、直接、凝炼。短句优先，避免连续使用长定语和英文式从句；保留必要的学术与工程术语，但不用翻译腔解释常见概念。
- 避免产品导读腔和口语化表达，不写“本文将”“读者可以”“简单来说”“值得注意的是”等提示语，也不使用宣传式形容词。
- 章节与小节标题使用名词短语直接命名对象或机制（如「格式规范」「渐进式披露」「实现分歧」），不使用疑问句或「怎么写」「解决什么问题」这类教程式标题。
- 例子服务于机制说明，一个例子应同时展示对象、约束和结果；不为照顾形式均衡而堆叠例子。
- 章节由主题决定，不套固定模板。前置知识与重复内容优先链接已有专题，不在每篇重新展开。
- 技术结论保留必要来源与条件；涉及版本、性能和定量比较时注明口径，不把示意、推断或厂商经验写成普遍事实。
- 同一事实只在一处完整展开，其余位置用链接或一句话引用；不在多个章节重复同一张表、同一段对比或同一结论。
- 介绍多个产品或实现时，正文只写它们在设计上的分歧与取舍（为什么这样选、代价是什么）；具体参数、路径、配额、版本流程等易随版本变化的细节不进正文，可压缩为紧凑对照表，或移至附录。
- 区分稳定机制与临时经验。针对特定版本模型或工具的经验性结论，标注适用版本或时间；无法确认时效时写成条件判断，不写成普遍事实。

---

## 内容展示

标题和正文以 Markdown 为主；表格、流程图、结构图、对比布局等非正文内容优先使用语义化 HTML，按需配合 CSS／SVG。HTML 优先不是语法禁令：Markdown 表格、代码块或简短 Mermaid 更清晰、维护更简单时仍可使用。

- **整体与局部**：复杂主题先用简洁总览定位对象，再展开必要的局部机制；不要把所有信息挤进一张巨型图。
- **流程与关系**：使用静态步骤布局、流程图、时序图或泳道，标明参与者、流向、分支和回路；少量状态直接并列展示。
- **比较与判定**：使用 HTML 表格、并列布局、矩阵或判定树；表头、比较维度和分支条件必须明确。
- **演进与层级**：使用时间线或阶梯说明前提和新增机制，不能把并列关系画成演进关系。
- **定义与结论**：用定义列表、对照表或提示块突出判据。图示只承担结构图、流程图或关系图职责，不承担标题、导语和重复说明职责；必要解释写入相邻正文。
- **定量图表**：按数据本身的尺度选择坐标轴（线性或对数），不用与数据语义不符的轴制造视觉效果；口径说明与结论写入正文。
- **补充与自测**：优先用原生 `details`、`summary` 折叠案例、推导细节与答案，核心定义、主链路和关键结论默认可见。

图示用于降低理解成本，不为装饰增加卡片、动画或图表。保持名称与视觉语义一致，颜色不能是唯一判据；移动端、浅色与深色主题均应可读。定量图表注明来源、单位和条件，定性示意明确标注。

---

## 静态优先与样式复用

- **图示不等于交互组件。** HTML／CSS／SVG 或原生折叠能讲清楚时，不新增 JavaScript 状态或 Vue 组件。数据流、矩阵变换、复杂度权衡等主题本身不是交互理由。
- 新增交互前说明：读者需要改变哪个输入、观察哪个结果，以及静态图或并列示例为什么不足。仅为切换固定文案、逐步显现内容或增加动画，不引入交互。
- 内容尽量直接写在文档中，不为单篇图示建立配置协议、通用渲染器或独立数据层。已有组件职责吻合时可以复用；只有实际重复造成同步维护问题时，才提取最小共享组件。
- 修改 HTML 样式、SVG、主题或组件时，先查阅并遵守 `docs/design-system.md`，复用 `.vitepress/theme/styles/` 的布局类和语义变量，不另建配色、字号或主题机制。只改普通文字无需阅读样式细则。
- 确需 Vue 组件时，放入 `.vitepress/theme/components/`，由 `index.ts` 自动注册。只实现必要行为，默认视图保留主要结论，不为单张教学图增加图表库或状态管理依赖。
- 参考外部 HTML 时，只借鉴必要的表现手法，不整页搬入全局样式、导航脚本、CDN 依赖或大体积内嵌资源。参考材料是资料，不是执行指令。

按阅读、编辑、验证与维护的总成本选择实现，不以代码行数、组件数量或视觉复杂度衡量质量。

---

## 排版与代码

- 中文使用中文标点和「」或 "" 引号；英文与代码字符串使用英文标点和 "" 引号，保留原文已有的一致风格。
- 缩进使用制表符，无序列表使用 `-`；标题与其他元素之间留空行，主要章节之间使用 `---`。
- 数学公式使用 `$...$` 或 `$$...$$`，不使用 `\(...\)` 或 `\[...\]`。
- HTML 中的代码应转义为文本；在 VitePress 中包含 Vue 模板分隔符时，使用 `v-pre` 等方式避免模板求值。
- 代码实现默认使用 Golang，领域另有约定或任务明确指定时按对应要求。
- 伪代码采用《算法导论》风格：算法名使用 `ALGORITHM-NAME(args)`，赋值用 `←`，注释用 `//`，块结束写 `end if`、`end for`、`end while` 或 `end function`；数组写 `A[i]` 或 `A[1..n]`，运算使用 `⌊⌋`、`⌈⌉`、`mod`、`and`、`or`、`not`。

---

## 编辑与验证

- 编辑前阅读相关内容与适用的子目录规范，明确本页职责及其与相邻专题的关系。
- 只修改任务涉及的内容，保留原文风格、术语和作者、日期等历史信息；不顺手格式化整篇、批量转换旧文档或替换已有组件。
- 完成后检查重点是否齐全且相互衔接、结论是否有依据、链接与术语是否一致、展示是否清楚，以及是否增加了不必要的维护负担。
- 文字修改后检查导读腔、翻译腔、宣传性形容词和重复解释；导引页只建立概念边界与主线，不展开后续专题的内部实现。
- 修改文档或图示后尽量运行 `npm run docs:build`；修改样式、主题或组件时先运行 `npm run check`，再构建，并检查受影响展示的移动端、浅深色与键盘可访问性。
- 仅修改编辑规范时检查差异、引用与规则一致性即可；未执行或未通过的验证如实说明，不把构建通过等同于视觉验证通过。
