# Embedding：离散对象的连续向量表示

如果希望先建立对 Embedding 整体结构的直观认识，可以先阅读下面的交互式结构图。该图概括了离散对象、查表映射、连续向量空间、静态/上下文化分支与下游任务之间的关系：

<EmbeddingMapExplorer />

---

> 相关文献：
> - Bengio et al. (2003)：在神经概率语言模型中系统引入可学习词向量。
> - Mikolov et al. (2013)：通过 word2vec 展示基于局部预测学习高质量词向量的高效路径。
> - Pennington, Socher, and Manning (2014)：通过 GloVe 展示全局共现统计与向量表示之间的关系。
> - Devlin et al. (2019)：以 BERT 为代表，推动上下文化 embedding 成为主流表示方式。

本文主要负责说明 embedding 的统一定义、几何直觉、学习方式与静态/动态表示差异，重点回答“离散对象如何进入连续空间”。与文本表示训练路线强相关的内容，应继续阅读 [text-embedding-training.md](./text-embedding-training.md)；与检索系统强相关的内容，应继续阅读 [retrieval-embedding.md](./retrieval-embedding.md)；与跨模态对齐强相关的内容，应继续阅读 [multimodal-alignment.md](./multimodal-alignment.md)。

---

## 符号约定与公式索引

为避免术语混乱，本文统一使用以下记号：

| 符号 | 含义 |
| --- | --- |
| $V$ | 离散对象的集合，如词表、商品集合、用户 ID 集合 |
| $\lvert V \rvert$ | 集合大小 |
| $w$ | 一个离散符号，通常表示词或 token |
| $i$ | 离散对象的索引 |
| $e_i\in\mathbb{R}^{\lvert V\rvert}$ | 第 $i$ 个对象的 one-hot 向量 |
| $d$ | embedding 维度 |
| $E\in\mathbb{R}^{\lvert V\rvert\times d}$ | embedding 矩阵，第 $i$ 行是对象 $i$ 的向量 |
| $x_i\in\mathbb{R}^d$ | 对象 $i$ 的 embedding 向量 |
| $s(i,j)$ | 对象 $i,j$ 的相似度函数 |
| $\theta$ | 除 embedding 外的其余模型参数 |
| $\mathcal{L}$ | 训练目标函数 |

本文核心公式索引：

1. one-hot 到 embedding 的查表形式：
$$
x_i = E^\top e_i
$$

2. 两个对象的点积相似度：
$$
s(i,j)=x_i^\top x_j
$$

3. 基于 softmax 的条件概率形式：
$$
P(j\mid i)=\frac{\exp(x_i^\top y_j)}{\sum_{k\in V}\exp(x_i^\top y_k)}
$$

4. 作为神经网络输入的嵌入层映射：
$$
h=f(Wx_i+b)
$$

下文依次讨论正式定义、核心机制、最小例子、专题分流、方法边界与历史定位。

---

## 问题背景与核心动机

Embedding（嵌入）是现代人工智能与机器学习中的基础技术。其基本思想，是将单词、图像、用户 ID 等离散对象映射为连续向量空间中的实数向量，从而使计算机能够以统一的数值形式处理原本抽象、离散的概念。

对于计算机而言，「苹果」「猫」这类概念本身并不能直接参与计算。为了让模型进行存储、比较、检索与优化，我们必须先把这些对象转换为数字表示。Embedding 的关键意义正在于：它不仅完成了编码，还在数值空间中赋予了离散对象可比较、可组合的结构。

### 为什么需要 Embedding

在 Embedding 广泛使用之前，常见方法是 **One-Hot Encoding（独热编码）**。这种表示简单直接，但存在两个根本局限：

1. **维度过高且极度稀疏**：若词表大小为 10 万，则每个词都需要一个 10 万维向量表示，存储与计算代价都很高。
2. **缺乏语义结构**：在 one-hot 表示中，「猫」与「狗」的向量彼此正交，模型无法仅凭表示本身判断它们同属动物，或在语义上彼此接近。

Embedding 的重要改进，在于将高维稀疏的离散表示压缩为低维稠密向量，并在向量空间中保留语义相似性与结构关系。

### Embedding 的核心特性

- **语义近邻性**：意义相近的对象在向量空间中通常距离更近；
- **维度压缩性**：可将成千上万维的离散特征压缩到几十维或几百维；
- **关系可计算性**：向量之间的加减、点积与夹角可以表达某些结构关系，例如
$$
\text{Vector}(\text{King}) - \text{Vector}(\text{Man}) + \text{Vector}(\text{Woman}) \approx \text{Vector}(\text{Queen})
$$

这表明 embedding 在一定条件下能够编码对象之间的关系模式，而不仅仅是孤立地表示单个对象。

---

## 正式定义与建模假设

Embedding 的核心目标，是把原本离散、不可直接比较的符号映射到连续向量空间中，使模型可以用线性代数运算来表达“相似、相关、可替换、可组合”等关系。

它本身通常不是一个独立完成任务的模型，更准确地说，它是现代机器学习与自然语言处理中的**基础表示层**。文本中的词、子词、位置编号、类别标签、推荐系统中的用户与物品 ID，都可以先经过 embedding，再交给后续模型处理。

Embedding 相对 one-hot 表示的核心改进在于：

- **从离散编码转向连续表示**：one-hot 只表示“是不是同一个对象”，而 embedding 允许表达“像不像”；
- **从参数独立转向参数共享**：相近对象可以在向量空间中靠近，从而共享统计强度；
- **从高维稀疏转向低维稠密**：输入维度显著降低，更适合神经网络优化；
- **从符号查表转向结构表达**：向量之间的距离、方向、夹角可以编码语义或行为模式。

Embedding 隐含的建模假设通常包括：

- **分布式表示假设**：对象的属性不是由单个维度独占表示，而是分散编码在多个维度中；
- **邻近可迁移假设**：语义、功能或行为相近的对象，其向量应当彼此靠近；
- **任务驱动假设**：embedding 的几何结构不是预先定义的，而是在某个训练目标下学出来的；
- **低维压缩假设**：高维离散空间中的有效结构，可以在较低维的连续空间中近似保留。

---

## 核心机制：从查表到几何结构

Embedding 最基本的形式，可以看作一个可学习的查表矩阵。若对象 $i$ 的 one-hot 表示为 $e_i$，embedding 矩阵为 $E$，则其向量表示为：

$$
x_i = E^\top e_i
$$

由于 $e_i$ 只有第 $i$ 位为 1，这个运算本质上等价于“取出矩阵 $E$ 的第 $i$ 行”。因此，embedding 层在计算上不是一般意义的矩阵乘法，而是**按索引查参数表**。

### 从 one-hot 到稠密向量

设词表大小为 $\lvert V\rvert=50000$，若直接用 one-hot 作为输入，每个词都对应一个 50000 维稀疏向量。这样做存在两个问题：

- 维度极高，大多数位置恒为 0；
- 任意两个不同词的内积都为 0，无法表达相似性。

Embedding 通过一个 $\lvert V\rvert\times d$ 的矩阵把词映射到 $d$ 维空间，通常 $d\ll \lvert V\rvert$。这样每个词都变为一个可训练的稠密向量。

### 相似度与几何关系

一旦对象被映射到向量空间，就可以定义相似度。最常见的形式包括：

- 点积：
$$
s(i,j)=x_i^\top x_j
$$
- 余弦相似度：
$$
\cos(i,j)=\frac{x_i^\top x_j}{\lVert x_i\rVert\lVert x_j\rVert}
$$

这些量不只是计算工具，也决定了 embedding 空间的几何含义：

- 距离近，通常表示上下文、功能或行为相似；
- 方向关系有时能反映某类可迁移模式；
- 模型最终利用的是“向量之间如何相互作用”，而不是单个对象的孤立标签。

### embedding 如何被学习

Embedding 本身没有固定训练方式。它的参数 $E$ 往往和整个任务一起通过反向传播学习。若某个下游任务的损失为 $\mathcal{L}$，则 embedding 矩阵中的被访问行会根据梯度更新：

$$
E \leftarrow E - \eta \frac{\partial \mathcal{L}}{\partial E}
$$

这意味着：

- 在语言模型中，embedding 学到的是“有助于预测上下文或下一个词”的表示；
- 在分类模型中，embedding 学到的是“有助于判别标签”的表示；
- 在推荐系统中，embedding 学到的是“有助于拟合用户-物品交互”的表示。

因此，embedding 的本质不是“给对象随便配一个向量”，而是让向量成为任务统计规律的可训练载体。

### 静态 embedding 与上下文化 embedding

从表示方式看，embedding 大致可分为两类：

| 类型 | 表示方式 | 特点 | 代表方法 |
| --- | --- | --- | --- |
| **静态 embedding** | 一个对象对应一个固定向量 | 简单、高效，但一词多义处理较弱 | word2vec、GloVe |
| **上下文化 embedding** | 同一对象在不同上下文下生成不同向量 | 表达力更强，可区分语境 | ELMo、BERT、GPT 系列中的隐层表示 |

例如，「bank」在 `river bank` 与 `open a bank account` 中，静态 embedding 往往只有一个固定向量；上下文化 embedding 则会根据上下文生成不同表示。

### 维度 $d$ 的选择

embedding 维度 $d$ 的选择，本质上是表示能力、泛化能力与计算代价之间的折中。维度过低时，模型难以容纳足够丰富的结构；维度过高时，参数量、显存开销与过拟合风险都会上升。

实践中常见约束包括：

- **内存与显存**：embedding 矩阵大小为 $\lvert V\rvert\times d$；
- **下游模型容量**：后续模型若较小，过高维度未必能被有效利用；
- **数据规模**：训练样本不足时，大维度更容易过拟合；
- **相似度度量稳定性**：极高维空间若缺乏归一化与正则，距离可能失去区分性。

因此，维度选择并不存在对所有任务都成立的固定最优值，更合理的做法通常是结合验证集效果、训练稳定性与资源预算逐步调整。

---

## 最小例子：从查表到语义关系

前几节给出的是抽象定义与机制描述。本节用一个极小例子把 embedding 的核心动作拆开来看：先看它如何完成“索引到向量”的映射，再看这些向量如何形成语义几何结构。

### 查表与矩阵乘法

假设词汇表为 `["我", "爱", "数学", "编程", "AI"]`，若要表示「数学」，其 one-hot 向量可写为
$$
x=[0,0,1,0,0]
$$

再设 embedding 矩阵为
$$
E=
\begin{bmatrix}
0.1 & 0.3\\
0.0 & 0.8\\
0.9 & 0.7\\
0.8 & 0.6\\
0.7 & 0.9
\end{bmatrix}
$$

则查表结果就是取出第 3 行：
$$
x_{\text{数学}}=[0.9,0.7]
$$

这个动作说明，embedding 层本质上并不是“重新计算一个复杂函数”，而是通过可学习矩阵把离散索引映射为稠密向量。

### 几何直觉

若进一步设：

- $x_{\text{数学}}=[0.9,0.7]$
- $x_{\text{编程}}=[0.8,0.6]$
- $x_{\text{苹果}}=[0.1,0.9]$

则「数学」与「编程」在空间中更接近，而与「苹果」距离更远。这个例子说明，embedding 相对 one-hot 的关键提升不只是“压缩维度”，更是把“相似性”“类别性”“关系性”写进了向量空间的几何结构中。

---

## 专题分流：训练路线、多模态与检索系统

到这里为止，本文已经回答了 embedding 作为统一表示层的核心问题：离散对象如何进入连续空间，向量之间如何形成几何关系，以及静态表示与动态表示的基本差异。

但从知识体系上看，下面 3 类内容虽然都与 embedding 强相关，却已经不再属于“统一定义主文档”的核心职责：

| 专题 | 主要问题 | 继续阅读 |
| --- | --- | --- |
| 文本表示训练路线 | embedding 如何通过局部预测、全局统计与上下文化目标学习出来 | [text-embedding-training.md](./text-embedding-training.md) |
| 跨模态对齐 | 图像、文本、音频等表示如何进入同一向量空间 | [multimodal-alignment.md](./multimodal-alignment.md) |
| 检索与向量系统 | embedding 如何进入 ANN、向量数据库、混合检索与重排流水线 | [retrieval-embedding.md](./retrieval-embedding.md) |

### 表示如何被学习出来

从训练视角看，embedding 既可以作为端到端任务的一部分联合训练，也可以先在大规模语料上预训练，再迁移到下游任务。若希望系统理解 Word2Vec、GloVe、FastText、ELMo、BERT、GPT 这条表示学习主线，应继续阅读 [text-embedding-training.md](./text-embedding-training.md)。

### 表示如何跨越模态边界

当表示对象不再只有文本，而扩展到图像、音频或视频时，问题会转化为“如何让不同模态在同一空间中彼此可比较”。这已经属于跨模态对齐专题，应继续阅读 [multimodal-alignment.md](./multimodal-alignment.md)。

### 表示如何进入检索系统

当 embedding 从训练阶段进入部署阶段，关注点会转向 ANN、向量数据库、混合检索、Late Interaction 与重排。这些问题更接近工程系统与服务化链路，应继续阅读 [retrieval-embedding.md](./retrieval-embedding.md)。

---

## 关键优势、局限与训练难点

Embedding 之所以有效，主要因为它把原本极其稀疏的离散统计问题，转化为连续空间中的参数共享问题。

它的主要优势包括：

- **缓解稀疏性**：相近对象共享表示结构，不再完全依赖精确匹配；
- **降低输入维度**：相较 one-hot，更适合大规模神经网络训练；
- **支持迁移与复用**：预训练向量可在多个任务中复用；
- **便于相似度计算**：可直接用于聚类、检索、召回与推荐；
- **兼容性强**：可作为几乎所有现代深度模型的前置表示层。

但 embedding 的局限也很明确：

- **语义依赖训练目标**：embedding 学到的“相似”未必符合人类语义直觉，而是服务于具体任务；
- **静态表示存在一词多义问题**：同一词在不同上下文中的意义会被混合；
- **长尾对象难以学好**：低频词、冷启动用户、稀有物品的向量质量常较差；
- **可解释性有限**：高维向量往往难以逐维解释；
- **可能继承数据偏差**：训练语料中的偏见会进入向量空间。

从训练角度看，embedding 常见难点包括：

- 高频对象更新过多，低频对象更新不足；
- 大词表或大规模 ID 集合会带来显著的存储与访存开销；
- 若训练目标过弱，向量可能只学到表面共现，而缺乏可迁移结构；
- 在检索或对比学习任务中，负样本构造方式会显著影响 embedding 几何形态。

---

## 与前后代模型的关系

从历史位置看，Embedding 不是某个单一时代的短期技巧，而是现代表示学习的基础接口。

在它之前，许多模型主要依赖：

- one-hot 编码；
- 离散计数特征；
- 手工设计的符号特征或统计特征。

这些方法的共同问题在于：特征之间缺少连续结构，泛化能力弱。Embedding 的引入，意味着模型开始默认采用“先学表示，再学决策”的路径。

在它之后，表示学习沿着两条路线继续发展：

- **静态分布式表示**：如 word2vec、GloVe，把每个词映射为固定向量；
- **上下文化表示**：如 ELMo、BERT、GPT，把 embedding 与上下文编码过程进一步耦合。

可以用下表概括其演化位置：

| 表示方式 | 是否连续 | 是否上下文化 | 参数共享能力 | 典型问题 |
| --- | --- | --- | --- | --- |
| **one-hot** | 否 | 否 | 几乎没有 | 稀疏、不可泛化 |
| **静态 embedding** | 是 | 否 | 强 | 一词多义、语境不足 |
| **上下文化 embedding** | 是 | 是 | 更强 | 计算代价更高 |

因此，Embedding 解决的不是“所有建模问题”，而是一个更基础的问题：如何让离散对象以适合优化与泛化的方式进入模型。

---

## 小结

Embedding 的最关键思想，是把离散符号映射到低维连续向量空间，使模型能够通过向量几何关系表达相似性、相关性与组合结构。

在模型演化史上，它的重要性不在于单独构成一个完整架构，而在于它奠定了现代表示学习的基本入口。无论是 NPLM、word2vec、RNN、Transformer，还是推荐系统、多模态模型与检索系统，它们之所以能高效处理大规模离散对象，几乎都建立在 embedding 这一连续表示机制之上。
