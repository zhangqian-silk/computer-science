# Task Scheduler Component

任务调度组件用于统一管理延迟任务、定时任务与批处理任务，保障任务按预期触发与执行。

---

## 组件边界

- 负责：任务建模、触发调度、执行派发、重试死信与调度可观测。
- 不负责：复杂工作流编排、业务幂等逻辑设计。

---

## 核心知识点清单

- 任务模型：一次性任务、周期任务、延迟任务。
- 调度模型：时间轮、优先队列、分片调度。
- 执行语义：至少一次、幂等执行、去重策略。
- 失败处理：重试、退避、死信、人工介入。
- 分布式协调：主备调度、抢占机制、锁协调。
- 观测指标：触发延迟、执行成功率、堆积深度。
