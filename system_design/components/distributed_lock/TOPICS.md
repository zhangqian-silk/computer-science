# Distributed Lock Component

分布式锁组件用于在多实例并发场景下协调互斥访问，避免共享资源竞争导致的数据错乱。

---

## 组件边界

- 负责：加锁、续约、解锁、锁超时控制与可观测。
- 不负责：业务事务补偿与分布式事务编排。

---

## 核心知识点清单

- 锁语义：互斥锁、可重入锁、读写锁、租约锁。
- 实现方式：Redis、数据库、ZooKeeper/etcd。
- 安全释放：唯一 token 校验与原子解锁。
- 锁续约与过期策略（看门狗机制）。
- 死锁与锁漂移风险识别。
- 指标体系：等待时长、成功率、超时率、冲突率。
