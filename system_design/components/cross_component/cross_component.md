# 跨组件协同（Cross-Component Collaboration）

> **摘要**：单个组件设计得再好，多个组件组合成系统时仍会出现新问题：**依赖拓扑复杂、变更相互影响、故障级联、SLO 预算相互挤占**。本专题不讲单组件算法，而讲「多个基础组件联合交付」时的控制面：依赖建模、变更编排、故障协同、预算治理。它是连接[组件能力层]与[工程治理层](../../governance/TOPICS.md)的桥梁。

> **说明**：与其他组件不同，跨组件协同是一种「协同方法」而非可独立复用的算法模块，因此本页以交互推演 + 方法论为主，不单独提供 demo 代码；相关机制的实现见[熔断](../../base/high_availability/circuit_breaker.md)、[降级](../../base/high_availability/degradation.md)等页。

---

## 一、依赖拓扑与关键路径

把组件间「谁调用谁」画成有向图，是协同治理的起点：

- **关键路径**：入口到核心结果必经的链路，任一环故障都会影响主功能，需最高保障等级。
- **强依赖 vs 弱依赖**：强依赖故障必须阻断或降级；弱依赖故障应可被跳过（如推荐、统计）。区分二者是降级设计的前提。
- **版本兼容窗口**：上下游同时演进时，接口需保证「新旧并存」的兼容窗口（先扩展后收缩，见下文变更编排）。

---

## 二、故障协同：不让单点拖垮全局

跨组件最大的风险是**故障级联**：底层一个依赖变慢/不可用，调用方线程/连接被占满，逐层向上传导，最终拖垮入口（雪崩）。用下面的组件感受级联与隔离的区别：

<FailurePropagationExplorer />

三道隔离防线（实现细节见对应文档）：

- **[熔断](../../base/high_availability/circuit_breaker.md)**：依赖错误率过高时快速失败，不再把请求打给故障依赖。
- **舱壁隔离**：为不同依赖分配独立线程池/连接池，一个依赖被打满不拖垮其他。
- **[降级](../../base/high_availability/degradation.md)**：弱依赖故障时返回兜底值，保住主链路。

---

## 三、变更编排

多组件联合发布时，变更顺序错误会造成短暂不兼容。核心原则：

- **扩展/收缩（expand/contract）**：先让下游兼容新旧两种输入（扩展），再切上游，最后移除旧路径（收缩）。数据库/接口变更都适用。
- **发布顺序**：一般「先发被依赖方（提供新能力），再发依赖方（使用新能力）」；下线相反。
- **冻结窗口与回滚门禁**：大促等高风险期冻结变更；每次发布预置可回滚点（见[灰度发布与回滚](../../governance/release_and_rollback.md)）。

---

## 四、SLO 预算协同

一次用户请求的可用性是链路上各组件可用性的乘积：$A_{\text{链路}} = \prod_i A_i$。5 个 99.9% 的串联组件，整体只有约 $99.5\%$。这决定了：

- 关键链路的组件要有更高的单点 SLO，或用冗余/降级把「串联」变「可容错」。
- **错误预算联动**：某组件错误预算透支时，联动收紧其上游的发布节奏、触发降级，避免继续放大（见[SLA/SLO 管理](../../governance/sla_slo_management.md)）。

---

## 五、观测与应急协同

- **链路追踪**：统一 traceId 贯穿所有组件，快速定位级联故障的源头。
- **统一告警分级**：避免每个组件各自告警造成风暴；按业务影响分级、按值班链路升级。
- **联合演练**：跨组件故障演练（见[混沌工程](../../governance/chaos_engineering.md)），验证隔离与降级在真实拓扑下有效。

---

## 六、面试高频问答

- **为什么底层依赖故障会拖垮整个系统？** 调用方资源（线程/连接）被慢依赖占满，逐层级联成雪崩。
- **怎么阻断级联？** 熔断快速失败 + 舱壁隔离资源 + 降级兜底。
- **串联多组件的整体可用性如何估算？** 各组件可用性相乘，串联越多整体越低，需冗余/降级。
- **多组件发布顺序怎么定？** 先发被依赖方再发依赖方；用 expand/contract 保证兼容窗口。
- **如何快速定位跨组件故障根因？** 统一 traceId 链路追踪 + 分级告警。

---

## 七、引用关系

- 边界与知识点索引：[`TOPICS.md`](./TOPICS.md)
- 依赖机制：[熔断](../../base/high_availability/circuit_breaker.md)、[降级](../../base/high_availability/degradation.md)、[负载均衡](../../base/high_availability/load_balancing.md)
- 工程治理：[SLA/SLO 管理](../../governance/sla_slo_management.md)、[灰度发布与回滚](../../governance/release_and_rollback.md)、[混沌工程](../../governance/chaos_engineering.md)、[可观测性](../../governance/observability.md)
